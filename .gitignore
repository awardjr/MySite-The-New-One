node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# CMS database (runtime data, edited via /admin — not shipped as source)
/data

# CMS-uploaded images (runtime data, uploaded via /admin — not shipped as source)
/static/uploads

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Tools
.vscode
.idea
